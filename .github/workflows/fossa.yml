# name: FOSSA Image Scan

# on:
#   push:
#     branches: [ test/trivy-scan ]

# jobs:
#   fossa-image:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Build Docker Image
#         run: docker build -t my-app:latest .

#       - name: Install FOSSA CLI v3
#         run: |
#           curl -H 'Cache-Control: no-cache' https://raw.githubusercontent.com/fossas/fossa-cli/master/install-latest.sh | bash
#           echo "$HOME/.local/bin" >> $GITHUB_PATH

#       - name: Scan Docker Image
#         env:
#           FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}
#         run: |
#           fossa container analyze my-app:latest --project security-guardrails-poc
#           fossa test --project security-guardrails-poc

# name: FOSSA Image Scan

# on:
#   push:
#     branches: [ test/trivy-scan ]

# jobs:
#   fossa-image:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Build Docker Image
#         run: docker build -t my-app:latest .

#       - name: Install FOSSA CLI v3
#         run: |
#           curl -H 'Cache-Control: no-cache' https://raw.githubusercontent.com/fossas/fossa-cli/master/install-latest.sh | bash
#           echo "$HOME/.local/bin" >> $GITHUB_PATH

#       - name: Scan Docker Image
#         env:
#           FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}
#         run: |
#           fossa container analyze my-app:latest \
#             --project security-guardrails-poc \
#             --revision $GITHUB_SHA

#           fossa test \
#             --project security-guardrails-poc \
#             --revision $GITHUB_SHA

name: FOSSA Image Scan

on:
  push:
    branches: [ test/trivy-scan ]

jobs:
  fossa-image:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t my-app:latest .

      - name: Install FOSSA CLI v3 (latest)
        run: |
          curl -H 'Cache-Control: no-cache' https://raw.githubusercontent.com/fossas/fossa-cli/master/install-latest.sh | bash
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Verify FOSSA CLI version
        run: fossa --version

      # ── Analyze Go source dependencies (catches go.mod/go.sum deps) ──────
      - name: Analyze Source
        env:
          FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}
        run: |
          fossa analyze \
            --project security-guardrails-poc \
            --revision $GITHUB_SHA \
            --branch $GITHUB_REF_NAME

      # ── Analyze container image (catches OS/Alpine layer) ─────────────────
      - name: Save Docker image to tar
        run: docker save my-app:latest -o image.tar

      - name: Analyze Container Image (OS layer)
        env:
          FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}
        run: |
          fossa container analyze image.tar \
            --project security-guardrails-poc \
            --revision $GITHUB_SHA \
            --debug

      # ── Gate: fail if any security or license issues found ────────────────
      - name: FOSSA Test (fail on issues)
        env:
          FOSSA_API_KEY: ${{ secrets.FOSSA_API_KEY }}
        run: |
          fossa test \
            --project security-guardrails-poc \
            --revision $GITHUB_SHA

      - name: Upload FOSSA report URL
        if: always()
        run: |
          echo "FOSSA Report: https://app.fossa.com/projects/custom%2B28356%2Fsecurity-guardrails-poc/refs/branch/master/$GITHUB_SHA"